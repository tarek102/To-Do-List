import _ from 'lodash';
import './style.css';

function component() {
  let listItems = [
    {
      description: "Hello",
      completed: true,
      index: 0
    }
  ];
  
}

document.body.appendChild(component());